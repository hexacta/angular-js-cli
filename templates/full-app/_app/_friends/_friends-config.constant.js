(function () {
    'use strict';

    var friendsConfig = {
        // Specific module's configuration
    };

    angular
        .module('app.friends')
        .constant('FriendsConfig', friendsConfig);
})();
